<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Artiface" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Artiface.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFF6EE</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Artiface</item>
    </style>
</resources>
